{% extends 'SedaBundle::layout.html.twig' %}


{% block contenido %}
	
	{{ form_start(formulario, { 'attr': {'novalidate': 'novalidate', 'class': 'form-horizontal', 'id': 'usuarios'} }) }}
		    {{ form_errors(formulario) }}

	   <div class="row">
		    <div class="form-group">
		    	<div class="col-md-3 control-label"><strong>Usuario: </strong></div>
		        <div class="col-md-7 "><strong>{{ usuario.username }}</strong></div>
		    </div>
	    </div>
	   <div class="row">
		    <div class="form-group">
		    	<div class="col-md-3 control-label">{{ form_label(formulario.password, 'Contraseña: ') }}</div>
		        <div class="col-md-7">{{ form_widget(formulario.password, { 'attr': {'class': 'form-control'} }) }}</div>
			    <div class="alert-danger" align= "center">{{ form_errors(formulario.password) }}</div>
		    </div>
	    </div>	    
		<div class="form-group"><br></div>
	    <div class="row" align="center">
			<input class="btn btn-primary" type="submit" value="Guardar" />
        	&nbsp;&nbsp;
    	    <input type="button" class="btn btn-primary" onclick="location.href='{{ path('usuario_index') }}'" value="Cancelar" />
		</div>
		{{ form_end(formulario) }}


{% endblock %}
