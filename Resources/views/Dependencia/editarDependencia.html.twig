{% extends 'SedaBundle::layout.html.twig' %}

{% block contenido %}
	
{#

{{ dependencia | ld }}

{{ formulario | ld }} 

#} 

		{{ form_start(formulario, { 'attr': {'novalidate': 'novalidate', 'class': 'form-horizontal', 'id': 'observaciones'} }) }}
		    {{ form_errors(formulario) }}


	    <div class="row">
		    <div class="form-group">
		    	<div class="col-md-3 control-label">{{ form_label(formulario.denominacion, 'Denominación') }}</div>	
		        <div class="col-md-7">{{ form_widget(formulario.denominacion, { 'attr': {'class': 'form-control'} }) }}</div>
			    <div class="alert-danger" align= "center">{{ form_errors(formulario.denominacion) }}</div>
		    </div>
		</div>
	    <div class="row">
		    <div class="form-group">
		    	<div class="col-md-3 control-label">{{ form_label(formulario.iniciales, 'Iniciales') }}</div>	
		        <div class="col-md-7">{{ form_widget(formulario.iniciales, { 'attr': {'class': 'form-control'} }) }}</div>
			    <div class="alert-danger" align= "center">{{ form_errors(formulario.iniciales) }}</div>
		    </div>
		</div>
	    <div class="row">
		    <div class="form-group">
		    	<div class="col-md-3 control-label">{{ form_label(formulario.codigo, 'Codigo') }}</div>	
		        <div class="col-md-7">{{ form_widget(formulario.codigo, { 'attr': {'class': 'form-control'} }) }}</div>
			    <div class="alert-danger" align= "center">{{ form_errors(formulario.codigo) }}</div>
		    </div>
		</div>				
	    <div class="row">
		    <div class="form-group">
		    	<div class="col-md-3 control-label">{{ form_label(formulario.activo, 'Activo') }}</div>	
		        <div class="col-md-7">{{ form_widget(formulario.activo, { 'attr': {'class': 'checkbox-inline'} }) }}</div>
			    <div class="alert-danger" align= "center">{{ form_errors(formulario.activo) }}</div>
		    </div>
		</div>		    
			</br>
			<div class="form-group"><br></div>

		    <div class="row" align="center">
				<input class="btn btn-primary" type="submit" value="Modificar" />
			</div>

		
		{{ form_end(formulario) }}


{% endblock %}
{% block javascripts %}
	{{ parent() }}
	<script type="text/javascript" src="{{ asset('js/odis.js') }}"></script>
{% endblock %}