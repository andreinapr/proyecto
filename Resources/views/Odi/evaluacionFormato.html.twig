{% extends 'SedaBundle::layout.html.twig' %}

{% block contenido %}

{# {{ odis | ld }} 

{{ formularioOdi.objetivos[0].peso | ld }}
{{ odi.objetivos[0].peso | ld }}


{{ formulario | ld }}
{{ odi | ld }}

#}
<div class="class row">
		<H2 align="center">EVALUACIÓN DEL DESEMPEÑO / EXPOSICION DE MOTIVOS</H2>
		<h3 align="center">ASIGNACION DEL RANGO  “EXCEPCIONAL”</h3>
</div>

<div class="row">
	
	<table class="table table-striped">
		<thead>
			<tr>
				<th>Funcionario</th>
				<th>Periodo</th>
				<th>Año</th>
			</tr>
		</thead>
		<tbody>
			<tr>
				<td align = "center">{{ odi.usuario }}</td>
				<td align = "center">
					{% if odi.periodo == 1 %}
					    1er. Semestre
					{% endif %}
					{% if odi.periodo == 2 %}
					    2do. Semestre
					{% endif %}						
				</td align = "center">
				<td>{{ odi.ano }}</td>
			</tr>
		</tbody>
	</table>

</div>


{{ form_start(formulario, { 'attr': {'novalidate': 'novalidate', 'class': 'form-inline', 'id': 'evaluacionformato'} }) }}
		    {{ form_errors(formulario) }}


<div class="row" align="center">
	<h4>Identifique los <strong>LOGROS ADICIONALES</strong> alcanzados, especifique  cual (es), cuando?  Y como?</h4>
	<div class="form-group">
		<div>{{ form_widget(formulario.logroadicional, { 'attr': {'class': 'form-control2'} }) }}</div>
		<div class="alert-danger" align= "center">{{ form_errors(formulario.logroadicional) }}</div>
    </div>			
</div>

<div class="row" align="center">
	<h4>Especifique  el <strong>IMPACTO</strong> de los logros adicionales alcanzados para el Departamento, Dirección o  para la gestión especifica de la Institución.</h4>
	<div class="form-group">
		<div>{{ form_widget(formulario.impacto, { 'attr': {'class': 'form-control2'} }) }}</div>
		<div class="alert-danger" align= "center">{{ form_errors(formulario.impacto) }}</div>
    </div>			
</div>

<div class="row" align="center">
	<h4>Identifique las <strong>COMPETENCIAS INDIVIDUALES</strong> más resaltantes en el supervisado que le facilitaron el alcance de los logros adicionales.</h4>
	<div class="form-group">
		<div>{{ form_widget(formulario.competenciasindividual, { 'attr': {'class': 'form-control2'} }) }}</div>
		<div class="alert-danger" align= "center">{{ form_errors(formulario.competenciasindividual) }}</div>
    </div>			
</div>

<div class="form-group"><br></div>
<div class="row">
	<input id="botonGuardar" class="btn btn-primary" type="submit" value="Guardar" /> 
	{# <button type="button" id="botonGuardar" class="btn btn-primary" >Guardar</button> #}
</div>

{{ form_end(formulario) }}




{% endblock %}
{% block javascripts %}
	{{ parent() }}
	<script type="text/javascript" src="{{ asset('js/evaluacionformato.js') }}"></script>
{% endblock %}