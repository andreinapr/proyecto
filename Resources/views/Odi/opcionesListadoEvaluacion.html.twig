{% extends 'SedaBundle::layout.html.twig' %}



{% block contenido %}
	
		{{ form_start(formulario, { 'attr': {'novalidate': 'novalidate', 'class': 'form-inline', 'id': 'opcioneslistadoevaluacion'} }) }}
		    {{ form_errors(formulario) }}
		<div class="row">

		</div>
	    <div class="row" align= "center">
	   	    <div class="form-group">
		    	<div>{{ form_label(formulario.tipo, 'Tipo de Personal') }}</div>	
		        {{ form_widget(formulario.tipo, { 'attr': {'class': 'form-control'} }) }}
			    <div class="alert-danger" align= "center">{{ form_errors(formulario.tipo) }}</div>        
		    </div>
	    </div>
	    
	    <div class="row" align= "center">

		    <div class="form-group">
		    	<div>{{ form_label(formulario.periodo, 'Periodo') }}</div>	
		        {{ form_widget(formulario.periodo, { 'attr': {'class': 'form-control'} }) }}
			    <div class="alert-danger" align= "center">{{ form_errors(formulario.periodo) }}</div>        
		    </div>

		    <div class="form-group">
		    	<div>{{ form_label(formulario.ano, 'Año') }}</div>	
		        <div>{{ form_widget(formulario.ano, { 'attr': {'class': 'form-control'} }) }}</div>
		        <div class="alert-danger" align= "center">{{ form_errors(formulario.ano) }}</div>
		    </div>

	    </div>
	    <div class="row" align= "center">
	   	    <div class="form-group">
		    	<div>{{ form_label(formulario.orden, 'Orden') }}</div>	
		        {{ form_widget(formulario.orden, { 'attr': {'class': 'form-control'} }) }}
			    <div class="alert-danger" align= "center">{{ form_errors(formulario.orden) }}</div>        
		    </div>
	    </div>
		<div class="form-group"><br></div>
	    <div class="row" align= "center">
			<input class="btn btn-primary" type="submit" value="Generar" />
		</div>
		{{ form_end(formulario) }}
	</br>

{% endblock %}
{% block javascripts %}
	{{ parent() }}
	<script type="text/javascript" src="{{ asset('js/opcioneslistadoevaluacion.js') }}"></script>
{% endblock %}