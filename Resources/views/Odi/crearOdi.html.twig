{% extends 'SedaBundle::layout.html.twig' %}

{% block contenido %}
	
{# 

{{ odi | ld }}

{{ formulario | ld }} 

 #}

		{{ form_start(formulario, { 'attr': {'novalidate': 'novalidate', 'class': 'form-inline', 'id': 'odis'} }) }}
		    {{ form_errors(formulario) }}

		<div class="row">
			<div class="alert-danger" align= "center">{{ error }}</div>
		</div>
	</br>
	    <div class="row">

		    <div class="form-group">
		    	<div>{{ form_label(formulario.usuario, 'Funcionario') }}</div>	
		        <div>{{ form_widget(formulario.usuario, { 'attr': {'class': 'form-control'} }) }}</div>
			    <div class="alert-danger" align= "center">{{ form_errors(formulario.usuario) }}</div>
		    </div>

		    <div class="form-group">
		    	<div>{{ form_label(formulario.periodo, 'Periodo', { 'attr': {'class': 'radio'}}) }}</div>	
		        {{ form_widget(formulario.periodo, { 'attr': {'class': 'radio'}}) }}
			    <div class="alert-danger" align= "center">{{ form_errors(formulario.periodo) }}</div>        
		    </div>

		    <div class="form-group">
		    	<div>{{ form_label(formulario.ano, 'Año') }}</div>	
		        <div>{{ form_widget(formulario.ano, { 'attr': {'class': 'form-control'} }) }}</div>
		        <div class="alert-danger" align= "center">{{ form_errors(formulario.ano) }}</div>
		    </div>

		    <div class="form-group">
		    	<div>{{ form_label(formulario.total_pesos, 'Total Pesos') }}</div>
		        <div>{{ form_widget(formulario.total_pesos, { 'attr': {'class': 'form-control', 'readOnly': 1, 'size': '17'} }) }}</div>
			    <div class="alert-danger" align= "center">{{ form_errors(formulario.total_pesos) }}</div>
		    </div>

	    </div>
	    <br>
	    <div class="row">

	    	<table class="table table-striped">
	    		<thead>
	    			<tr>
	    				<th>#</th>
	    				<th>Objetivos</th>
	    				<th>Peso</th>
	    				<th>Primera Revisión</th>
	    				<th>Segunda Revisón</th>
	    				<th>Tercera Revisión</th>
	    			</tr>
	    		</thead>
	    		<tbody>
					{% for objetivo in formulario.objetivos %}

		    			<tr>
		    				<td>{{ loop.index }}</td>
		    				<td>
						        <div>{{ form_widget(objetivo.objetivo, { 'attr': {'class': 'form-control1', 'rows': '3'} }) }}</div>
						        <div class="alert-danger" align= "center">{{ form_errors(objetivo.objetivo) }}</div>
		    				</td>
		    				<td>
					        	<div>{{ form_widget(objetivo.peso, { 'attr': {'class': 'form-control'} }) }}</div>
					        	<div class="alert-danger" align= "center">{{ form_errors(objetivo.peso) }}</div>
		    				</td>
		    				<td>
					        	<div>{{ form_widget(objetivo.primera_revision, { 'attr': {'class': 'form-control', 'readOnly': 1} }) }}</div>
					        	<div class="alert-danger" align= "center">{{ form_errors(objetivo.primera_revision) }}</div>
		    				</td>
		    				<td>
					        	<div>{{ form_widget(objetivo.segunda_revision, { 'attr': {'class': 'form-control', 'readOnly': 1} }) }}</div>
					        	<div class="alert-danger" align= "center">{{ form_errors(objetivo.segunda_revision) }}</div>
		    				</td>
		    				<td>
					        	<div>{{ form_widget(objetivo.tercera_revision, { 'attr': {'class': 'form-control', 'readOnly': 1} }) }}</div>
					        	<div class="alert-danger" align= "center">{{ form_errors(objetivo.tercera_revision) }}</div>

					        	<div hidden="hidden">{{ form_widget(objetivo.fecha_creacion, { 'attr': {'class': 'form-control', 'readOnly': 1} }) }}</div>
					        	<div hidden="hidden" class="alert-danger" align= "center" >{{ form_errors(objetivo.fecha_creacion) }}</div>

		    				</td>
		    			</tr>

					{% endfor %}
	    		</tbody>
	    	</table>

		<div class="form-group"><br></div>
	    <div class="row">
			<input class="btn btn-primary" type="submit" value="Guardar" />
		</div>


		{{ form_end(formulario) }}


{% endblock %}
{% block javascripts %}
	{{ parent() }}
	<script type="text/javascript" src="{{ asset('js/odis.js') }}"></script>
{% endblock %}